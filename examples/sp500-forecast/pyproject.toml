[project]
name = "sp500-forecast"
version = "0.1.0"
description = "S&P 500 7-day forecast — autoforge-ml example"
requires-python = ">=3.11"
dependencies = [
    "yfinance>=0.2",
    "scikit-learn>=1.4",
    "pandas>=2.0",
    "numpy>=1.26",
]
